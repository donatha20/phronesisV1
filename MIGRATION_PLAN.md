# Firebase → Django / PostgreSQL / AWS — Migration Plan & Tracker

## Decisions (locked)

- Backend: **Django 5 + Django REST Framework**
- Hosting: **AWS** — RDS PostgreSQL (Multi-AZ), ECS Fargate + ALB, S3 + CloudFront, ElastiCache Redis, SQS (Celery broker), Secrets Manager, ECR, Route 53 + ACM
- Auth: email/password primary **+ "Login with Google"** option (django-allauth OIDC, scopes `openid email profile`). JWT via SimpleJWT (15 min access / 14 day rotating refresh). Web = httpOnly cookies; Android = EncryptedSharedPreferences
- Google Workspace data (Drive/Calendar/Meet): **separate** server-side OAuth broker, refresh token encrypted at rest. Not the login flow
- Repo: **monorepo** (`apps/web`, `apps/android`, `services/api`, `packages/shared-types`, `infra/`)
- Spiritual Assistant: **deferred** — endpoint stubbed behind `ASSISTANT_FEATURE_ENABLED=false`
- Prayer vault v1: **access control + audit logging only** (no app-layer encryption); password re-auth before revealing `PRIVATE_VAULT` entries

## Firebase dependency inventory

### Web (`apps/web`)
| Ref | Item | Purpose | Replacement |
|-----|------|---------|-------------|
| W1 | `firebase` npm pkg | pulls firebase/app + firebase/auth | remove; `apiClient` fetch wrapper to Django |
| W2 | `initializeApp` (googleAuth.ts) | Firebase app singleton from `firebase-applet-config.json` | delete |
| W3 | `onAuthStateChanged` | reactive "is signed in" | session cookie + `GET /api/auth/user/` + React `AuthProvider` |
| W4 | `signInWithPopup`+`GoogleAuthProvider` | broker a Google OAuth **access token** w/ 12 Workspace scopes | server-side OAuth broker; client never sees Google token |
| W5 | `signOut` | clear session | `POST /api/auth/logout/` |
| W6 | `User` type | callback typing | local `AuthUser` from our API |
| W7 | `firebase-applet-config.json` | apiKey/authDomain/... + `oAuthClientId` | delete; reuse `oAuthClientId` as `GOOGLE_WORKSPACE_CLIENT_ID` (backend); rotate `apiKey` |
| W8 | `signInWithGoogleDrive()` callers (GoogleCalendarMeetView, GoogleDriveView) | trigger sign-in, hold token, call googleapis | call `/api/integrations/google/*` + `/api/sessions/` |
| W9 | in-memory token cache | hold Google token | gone (server-side only) |
| W10 | googleDriveService.ts | browser→Drive v3; `Blob.text()` corrupts binary | backend `/api/resources/` → S3 + Postgres |
| W11 | googleCalendarService.ts | browser→Calendar v3; hardcodes tomorrow 19:00 | backend persists real `scheduled_at`, calls Calendar w/ stored token |
| W12 | googleMeetService.ts | browser→Meet v2 spaces | backend creates space on session create |
| W13 | `App.tsx` useState arrays from sampleData | all app state | Postgres tables + REST |
| W14 | SpiritualAssistantModal canned replies | fake AI | `/api/assistant/query/` (deferred, flagged off) |

### Android (`apps/android`)
| Ref | Item | Status | Action |
|-----|------|--------|--------|
| A1 | `google-services` Gradle plugin (root + app + `googleServices{}`) | no `google-services.json` present | remove from both build files + catalog |
| A2 | `platform(libs.firebase.bom)` | version BOM | remove |
| A3 | `libs.firebase.ai` | declared, **zero code refs** | remove |
| A4 | `libs.firebase.appcheck.recaptcha` + `.debug` | declared, no init/usage | remove; use bearer-token API auth + rate limiting (+ Play Integrity later) |
| A5 | `secrets { ignoreList += FIREBASE_APPCHECK_DEBUG_TOKEN }` | reserved name | drop the line |
| A6 | `firebase-firestore`, `firebase-auth` catalog entries | commented / unused | delete entries |
| A7 | Retrofit/OkHttp/Moshi/Room deps | declared, unused | **keep** — target data layer |

Android has **no functional Firebase code**, only build wiring.

### Cannot migrate 1:1
- `onAuthStateChanged` reactive push → cookie + `/user/` fetch + context (multi-tab sync needs `storage` event/polling)
- `signInWithPopup` popup UX → full-page redirect; client loses direct Google token access (by design; fixes scope/refresh/browser-token issues)
- Firebase App Check / reCAPTCHA attestation (Android, inert) → no drop-in; substitute JWT auth + rate limiting + optional Play Integrity / Turnstile
- `firebase-ai` Gemini → new backend feature, not a migration item (no data/behavior lost)
- Firestore realtime listeners → none in use; if needed later use SSE/WebSocket/polling
- Prayer vault "encryption" (`pin === '1234'`, labels) → real access control + audit + password re-auth

## Phase tracker

- [x] **P0** Monorepo restructure (web→apps/web, android→apps/android; added services/api, infra, packages, .github)
- [x] **P1** Django scaffold: settings split (base/dev/test/prod), DRF, allauth 65 + dj-rest-auth + SimpleJWT (httpOnly cookie JWT, rotating refresh + blacklist), drf-spectacular, Dockerfile, docker-compose, CI (ruff + migration-drift + pytest). Email-first custom `User` (UUID PK, role), `SecuritySettings`, append-only `AuditLog` + `record()` service + audited login/logout views, `EncryptedTextField`, health/ready probes. 4 smoke tests green, `makemigrations --check` clean, ruff clean.
- [x] **P2** Models + migrations for all domains + `seed_demo` (non-prod only). Apps: `mentorship` (application + pairing w/ partial-unique constraints), `goals` (Goal + Milestone), `devotions` (Devotion + Comment + like through-tables), `media` (PodcastEpisode + like/save), `sessions` (`DiscipleshipSession` w/ real `scheduled_at` + Meet/Calendar id fields), `prayers` (PrayerRequest + PrayerIntercession; privacy enum, no app-layer crypto), `resources` (Resource + Bookmark + Enrollment), `integrations` (`GoogleWorkspaceToken` w/ Fernet-encrypted refresh token). Shared `LifeSphere`/`UserRole` in `apps/common/choices.py`; profile fields folded onto `User`. Migrations apply clean, `seed_demo` works, ruff + pytest green.
- [x] **P3** Auth end-to-end. **Backend:** hand-rolled server-side Google OAuth authorization-code flow (`apps/accounts/google_oauth.py`) — `GET /api/auth/google/authorize/` (state in short-lived httpOnly cookie) → Google → `GET /api/auth/google/callback` verifies ID token + `email_verified`, upserts user, sets JWT cookies via `set_jwt_cookies`, redirects to `FRONTEND_URL/auth/callback`; all failures redirect to `FRONTEND_URL/login?error=<slug>` with a `LOGIN_FAILED` audit row. `GET /api/auth/csrf/` primes the csrftoken cookie; `REST_AUTH` now enforces CSRF on cookie-JWT (`X-CSRFTOKEN`). `ACCOUNT_EMAIL_VERIFICATION="none"` (email verification deferred). 6 new backend tests (10 total green). **Frontend (`apps/web`):** `lib/api.ts` (credentialed fetch, CSRF header, 401→refresh→retry), `auth/AuthContext.tsx` (`AuthProvider`/`useAuth`: login, register, logout, refresh, `loginWithGoogle` full-page redirect), `auth/AuthScreen.tsx` (login/register + "Continue with Google"), `auth/CallbackScreen.tsx`, `auth/adapt.ts` (API user → legacy `UserProfile` bridge until P4). `App.tsx` gated on auth; `Header.tsx` persona-switcher replaced with real user + Sign out. **Firebase removed from web:** `firebase` dep dropped, `firebase-applet-config.json` deleted, `services/googleAuth.ts` rewritten as a Workspace-connection shim (real broker in P5). `tsc` + `vite build` green.
- [~] **P4** Core CRUD endpoints + RBAC + audit logging (**P4a backend done**); port App.tsx state to an API-backed store (**P4b frontend pending**).
  - **P4a:** `AuditedModelViewSet` base, `apps/common/permissions.py` (role helpers, `ReadOnlyOrMentor`, owner checks), `config/api_router.py`. ViewSets: goals (+nested milestones, mentor-approval gating), devotions (+like / comment / comment-like actions — comments now persist), episodes (+like/save, view-count), sessions (real `scheduled_at`, pairing-checked create, cancel/complete), prayers (privacy-scoped queryset; PRIVATE_VAULT body masked until `POST /api/prayers/vault/unlock/` password re-auth, 10-min window; intercede/answer; `PRAYER_VIEW` / `VAULT_UNLOCK` / `VAULT_UNLOCK_FAILED` audits), resources (access-tier queryset, bookmark/enroll), mentorship (application → accept creates `Mentorship` + bumps `active_mentees_count`, one-active-mentor rule), `GET /api/mentors/` directory. 31 tests green (24 new), ruff clean, OpenAPI generates.
  - **P4b (core views) done:** `@tanstack/react-query` added; `src/api/{dto,enums,adapters,hooks}.ts` (typed DTOs, snake_case↔legacy-enum maps, DTO→legacy-type adapters, query/mutation hooks with cache invalidation). Wired **Goals, Devotions, Prayers, Sessions** (+ Dashboard's read side) onto the live API in `App.tsx`; Podcasts/Mentors/Resources/Security/Google Drive & Calendar views remain on sample data pending their own pass. View contract changes: `SpheresGoalsView` (`onToggleMilestone`/`onCreateGoal` replace whole-object callbacks), `PrayerVaultView` (real password re-auth via `vaultUnlocked`/async `onUnlockVault`, removed the "1234"/biometric/zero-knowledge language), `DiscipleshipSessionsView` (schedules with the real paired mentor via `useMyMentorship`, `datetime-local` picker replaces free-text time, dead Google-sync branch removed), `DailyDevotionsView` (comments fetched per-devotion via `useDevotionComments` and posted through the API — comments now persist for real; added a like button). Found and fixed a real bug along the way: dj-rest-auth's `RegisterView` never set the JWT cookies it advertises in its response body — added `AuditedRegisterView` to set them + audit `REGISTER` (backend tests still 31/31 green; verified live via `APIClient`). `tsc` + `vite build` green.
  - **P4c (remaining views) done:** Podcasts, Resources, Mentor directory/application, and Security settings now on the live API too. Backend additions: `SecuritySettingsSerializer` + `GET/PATCH /api/auth/security/`, `AuditedPasswordChangeView` (`POST /api/auth/password/change/`, updates `last_password_change_at`, audits `PASSWORD_CHANGE`) — 3 new tests, 34 total green. Frontend: `useEpisodes`/`useEpisodeMutations`, `useResources`/`useResourceMutations`, `useMentorsDirectory`, `useMentorshipMutations`, `useUpdateMyProfile`, `useMySecuritySettings`/`useSecurityMutations`; `episodeToCreateInput`/`resourceToCreateInput` reverse-adapters let `PodcastMediaHubView`/`ResourceLibraryView` keep their existing object-literal create flow unchanged. `auth/adapt.ts` gained `blankProfileTemplate()` so `apiUserToProfile` works for arbitrary directory users, not just the signed-in one — kept as the permanent API-user→legacy-UI bridge rather than retired (the legacy `UserProfile` shape is still what every view renders).
  - **`MentorMatchingView` rewritten for real accounts:** the old "register a new mentor/mentee" affordance is gone (real signup is the auth screen now); mentors only edit *their own* bio (`PATCH /api/auth/user/`); "Choose Mentor" now creates a real `MentorshipApplication` (pending mentor acceptance) instead of instantly fabricating a pairing + session. Mentee directory stays on sample data — there's no authorized backend endpoint for browsing mentees (privacy), unlike the public mentor directory.
  - **`SecuritySettingsView` rewritten** to drop the fictional PIN/biometrics/"AES-256-GCM"/backup-date UI entirely, replaced with a real change-password form (`/api/auth/password/change/`), a two-factor *preference* toggle (explicitly labeled "not yet enforced"), and real last-unlock/last-password-change timestamps.
  - **Known gap:** `currentUser.assignedMentorId`/`assignedMentorName` (drives the "Your Mentor" badge in the directory) isn't populated from the real pairing yet — cosmetic only, tracked for a follow-up.
  - **Still sample-data:** mentee directory (privacy-gated, no endpoint), Google Drive/Calendar views (their real broker is phase P5). `packages/shared-types` (OpenAPI codegen) not yet wired — hand-written DTOs stand in.
- [x] **P5** Google Workspace broker (Drive/Calendar/Meet), server-side. `apps.integrations` (already scaffolded in P2) gained `google_oauth.py` (separate authorization-code flow from login — `access_type=offline` + `prompt=consent` so a refresh token is always granted) and `google_client.py` (builds a live `Credentials` from the encrypted refresh token, refreshing on every call; access tokens are never persisted). Endpoints under `/api/integrations/google/`: `status/`, `authorize/` (requires an existing Phronesis session — connecting never logs anyone in/out), `callback` (state+uid cookies, same pattern as `apps.accounts.google_oauth`), `disconnect/` (revokes at Google + marks `revoked_at`), `drive/files/` (list/delete), `drive/folders/`, `drive/upload/`, `calendar/events/` (list/create-with-optional-Meet/delete), `meet/instant/` (an instant Calendar event with `conferenceData` — the Meet v2 REST API needs separate Workspace-admin gating, so this is the standard way to mint a Meet link off just the Calendar scope). All proxy calls use `google-api-python-client`; failures surface as `409 not_connected` or `502 google_error`. 10 new backend tests (44 total green), ruff clean. **Frontend:** `services/googleAuth.ts` now exposes `getWorkspaceStatus`/`connectGoogleWorkspace` (full-page redirect, matching "Login with Google")/`disconnectGoogleWorkspace` — no more browser-held Google access token. `googleDriveService.ts`/`googleCalendarService.ts`/`googleMeetService.ts` rewritten to call our API instead of `googleapis.com` directly, keeping the same `GoogleDriveFile`/`GoogleCalendarEvent` shapes so `GoogleDriveView`/`GoogleCalendarMeetView` needed only their auth-lifecycle blocks and call sites updated (drop the `accessToken` threading, `isConnected: boolean` replaces it), not a rewrite. The connect/disconnect broker redirects to `/?google_connected=1` / `/?google_error=<code>`; `App.tsx` picks that up on mount, switches to the Drive tab, and scrubs the query string. `tsc` + `vite build` green.
- [x] **P6** S3 media/resources: real upload/download via presigned URLs. Production S3 storage (`storages.backends.s3.S3Storage`, `AWS_QUERYSTRING_AUTH=True`) was already wired in `config/settings/prod.py` since P1 — every `FileField.url` is already a time-limited signed GET, no work needed there. What was missing: uploads were never wired up on either end — the Resource/Podcast "upload" forms only ever built fake metadata (`fileSize: '4.2 MB'`, `durationSeconds: 1800` hardcoded) with no actual file. Added: `apps/common/storage.py` (`generate_presigned_post` — direct browser→S3 upload via presigned POST, only when `default_storage` is S3-backed) and a `presign-upload` action on both `ResourceViewSet` and `PodcastEpisodeViewSet` (`POST /api/resources/presign-upload/`, `POST /api/episodes/presign-upload/`; 501 `direct_upload_not_supported` in local/test dev, where there's no bucket to presign against). `ResourceSerializer`/`PodcastEpisodeSerializer` gained a write-only `file_key`/`media_file_key` for adopting an already-uploaded S3 object (computes `file_size_bytes` from a storage HEAD request) — and a `JSONStringFieldsMixin` (`apps/common/serializers.py`) so the array fields (`syllabus_chapters`, `key_scriptures`, etc.) still parse correctly when a request arrives as multipart (the local-dev fallback path) instead of JSON. **Frontend:** `api/directUpload.ts` (`uploadFileDirect`) tries the presigned-S3 path first and falls back to a plain multipart POST to the create endpoint on `501` — so the exact same UI code works against S3 in prod and the filesystem in dev, no environment branching in the view layer. `ResourceLibraryView`/`PodcastMediaHubView` gained real `<input type="file">` fields (the upload forms are no longer 100% fictional); episode duration is now actually parsed from the "mm:ss" input instead of a hardcoded 1800. 7 new backend tests (51 total), ruff clean; `tsc` + `vite build` green.
- [x] **P7** Prayer vault v1: privacy querysets + password re-auth + audit trail — done as part of P4a (`apps/prayers`), tracker just hadn't been updated.
- [x] **P12** Extensible roles + de-hardcoding pass (partial — see below). Prompted by a codebase audit that found: only 3 fixed roles (mentee/mentor/admin, a `TextChoices` enum) with **no in-app way to change a user's role** (Django admin only, unaudited, despite `AuditAction.ROLE_CHANGE` already existing unused); and several visible frontend features that were still fully fake despite most CRUD views already being wired to the live API.
  - **R1 (roles model):** New `accounts.Role` table (`name`, `slug`, `base_kind` mentee/mentor/admin, `capabilities`, `is_system`) — migration seeds the 3 original roles at a fixed pk so `User.role`'s FK default needs no DB query (a query-based default crashes Django's `check_user_model` system check on a fresh, unmigrated DB). `User.role` is now a FK; `Role.resolve()` lets every old call site that assigned a plain string (`RegisterSerializer`, Google OAuth callback, `seed_demo`, `UserManager`) keep working unchanged. `is_admin`/`is_mentor` (`apps/common/permissions.py`) now read `role.base_kind` — a one-line change each, not a rewrite of the 7 other apps' RBAC. Zero behavior change for existing users; 6 new tests confirm a custom role with `base_kind="mentor"` genuinely grants mentor permissions.
  - **R2 (admin API):** `RoleViewSet` (`/api/admin/roles/`, admin-only CRUD; system roles can't be deleted or have `slug`/`base_kind` changed) and `AdminUserViewSet` (`/api/admin/users/`, list/search + `PATCH .../role/` to assign a role) in `apps/accounts`. Role assignment now emits the long-dormant `AuditAction.ROLE_CHANGE` with old/new role in metadata — this used to only be possible, unaudited, via the Django admin panel. `UserSerializer` gained `role_base_kind`/`role_name` alongside the existing `role` (slug) so clients can tell a custom role's *kind* apart from its *name*. 10 new tests. 67 total backend tests green, ruff clean, no migration drift.
  - **R3 (admin UI):** New `AdminRolesView.tsx` (role list/create/delete + a searchable user list with a role-assign dropdown), gated behind a new `ADMIN` tab in `Header.tsx` shown only when `role_base_kind === 'admin'`. `api/hooks.ts` gained `useRoles`/`useRoleMutations`/`useAdminUsers`/`useAssignUserRole`. `auth/types.ts`'s `ApiUser.role` widened from a 3-value union to `string` (a custom role's slug can be anything) with `role_base_kind` as the new source of truth for `ROLE_MAP` in `auth/adapt.ts`.
  - **D1 (Dashboard de-hardcoding):** the "Assigned Senior Mentor" card no longer always shows a fabricated "Elder Thomas Bradley" — it now renders the real `pairedMentor` (already computed in `App.tsx` via `useMyMentorship()`, previously only passed to the Sessions view) with an honest empty state when unpaired. The 5-Spheres radar chart is now computed from the user's real `goals` (average `progressPercent` per sphere) instead of a static `[85,70,80,90,75]` array.
  - **D2 (podcast/media de-hardcoding):** `EpisodeDTO.media_file` (the real uploaded file — already returned by the API since P6, silently dropped) now flows through `toEpisode()` into `PodcastEpisode.mediaUrl`; `handlePlayAudioPodcast` uses it instead of a hardcoded stock ambient-sound URL. `VideoPlayerModal` now renders a real `<video>` for an uploaded file or an `<iframe>` for `videoEmbedUrl`, falling back to an honest "no video uploaded" state instead of a permanently-decorative play button. `PodcastMediaHubView`'s upload form no longer persists 3 fabricated "key takeaways" strings to every new episode regardless of content — replaced with a real textarea field.
  - **D3 (remaining cleanup) done:** `GoogleCalendarMeetView`'s "Senior Mentor" dropdown now lists the real `useMentorsDirectory()` mentors instead of 3 hardcoded names/emails — and the dead `onSchedulePlatformSession` bridge (which fabricated a fake `DiscipleshipSession` with a client-generated id and a non-existent `mentor_gcal` mentor id every time a calendar event was created) was removed entirely rather than wired to a real session create, since a free-form Google Calendar invitee isn't necessarily the user's actual paired mentor and the backend's session-create endpoint is pairing-validated — the two concepts don't honestly map onto each other. **Mentee directory is now real**: new privacy-scoped `MenteeDirectoryViewSet` (`GET /api/mentees/`) — a mentor sees only their own active mentees (via `Mentorship`), an admin sees everyone, anyone else (including a mentee) sees an empty list, since there's no legitimate reason to browse young believers' profiles at large (this is why it was deferred in P4b/P4c). `useMenteesDirectory()` hook added; `data/sampleData.ts` deleted entirely (was down to one still-used export). `MentorMatchingView`'s "Young Believers" tab now only renders for mentors/admins. **Header's "24d Streak" is now real**: new `apps.devotions.DevotionReadLog` model (one row per user per devotion opened) + `POST /api/devotions/{id}/mark_read/` + `compute_streak_days()` (consecutive calendar days ending today-or-yesterday) exposed as `UserSerializer.devotion_streak_days` — computed only for the signed-in user's own record to avoid an N+1 in directory listings. Frontend calls `mark_read` when a devotion is opened (`DailyDevotionsView`'s select handler) and refreshes the auth context so the header updates. Minor cosmetic fixes: `ResourceLibraryView`'s fake `rating: 5.0` on new uploads now matches the server's real default (0, no rating feature exists), `PrayerVaultView`'s "Shared with Mentor (Elder Thomas)" now uses the real paired mentor's name, dead `assignedMentor` state in `SpheresGoalsView` deleted. 10 new backend tests (77 total), ruff clean, no migration drift, tsc/build clean.
  - **D4a (`LiveSessionCallModal`) done — real Meet links, honest UI, no fake call simulation.** Backend: `POST /api/sessions/{id}/generate-meet-link/` mints a real Google Meet link via the caller's connected Google Workspace (`apps.integrations.google_client`, same broker as P5) for the session's actual `scheduled_at`/`duration_minutes`, and persists it to `meet_url`/`meeting_link`/`google_calendar_event_id` (fields that had existed since P2 with a comment claiming they'd be "populated server-side by the Google Workspace broker" — nothing had ever actually done so). Idempotent (a session that already has a link is returned unchanged); `409 not_connected` if the caller hasn't connected Google Workspace. 4 new backend tests (81 total), ruff clean. **Frontend:** the modal no longer fakes a call at all — deleted the mic/camera toggle buttons that controlled nothing, the two avatar video tiles with hardcoded "Austin, TX"/"Seattle, WA" locations and pulsing "Audio Active"/"Speaking" badges, and the duration counter that just ticked a local timer starting at 128s. Replaced with an honest "Join Google Meet" button (real link) or a "Generate Google Meet Link" button (calls the new endpoint, surfaces the not-connected case with a pointer to the Google Drive tab) when there isn't one yet. Also fixed a real hooks-rule bug found while rewriting it: the original called `useState` after an early `if (!session) return null`, a conditional-hooks violation. The shared notes/scripture/action-items panel (the genuinely real part — notes persist via `onCompleteSession`) is unchanged. `App.tsx` now tracks the open call by session id and derives the live session object from the `sessions` query (was a stale snapshot) so a freshly generated link appears immediately. tsc + vite build clean.
  - **Not done (tracked for later):** D4b — the keyword-matched (non-AI) `SpiritualAssistantModal`. A real fix needs an LLM-backed endpoint, which needs a provider decision (e.g. Claude API + key) before any code is meaningful — not started.
- [ ] **P8** Harden: throttling, Sentry, WAF, RDS backup/restore rehearsal, load test
- [ ] **P9** Infra: Terraform for AWS; staging + prod; GitHub Actions deploy w/ gated migrate
- [ ] **P10** Android: strip Firebase Gradle wiring; Retrofit + Room against OpenAPI; real login
- [ ] **P11** Cutover: deploy prod, smoke test, disable Firebase project, rotate keys

## Post-cutover cleanup
- Rotate/disable Firebase `apiKey` in `apps/web/firebase-applet-config.json`
- Delete Firebase project `ai-studio-applet-webapp-c57ff`
